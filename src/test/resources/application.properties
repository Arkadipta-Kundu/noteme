# Disable MongoDB auto-configuration for tests
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.data.mongo.MongoAutoConfiguration,org.springframework.boot.autoconfigure.mongo.MongoAutoConfiguration

# Test database configuration (no actual connection needed for context loading test)
spring.data.mongodb.uri=mongodb://localhost:27017/test
spring.data.mongodb.database=test
